name: KiCAD export

on:
  - pull_request
  - push

jobs:
  kicad-export:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - run: ls
